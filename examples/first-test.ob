fn greet(string name) int {
    println(name);
    return 0;
}

// fn fib(int n) int {
//     if (n < 1) {
//         return 1;
//     }

//     return fib(n - 1) + fib(n - 2);
// }

fn add(int x, int y) int {
    return x + y;
}

fn main() int {
   bool y = false;
   string z = "Hello";

    switch (y) {
        case true:
            println("x is 5");
        case false:
            println("x is 6");
        default:
            println("case not found!");
    }

    if (z == "Hello") {
        println("z is hello");
    }

    int i = 0;
    while (i < 1) {
        println("Hello, world");
        i++;
    }

    for (int j = 0; j <= 5; j++) {
        println("Hello, world!");
    }

    // println(fib(10));

    int yy = 5;
    float fyy = cast(yy, float); // 5 -> 5.5

    println(typeof(fyy));
    println(typeof(yy));
    println(sizeof(float));

    println(add(5, 5));
    greet("Azlo");
    
    return 0;
} 